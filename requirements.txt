# 웹 프레임워크
fastapi>=0.104.0

# ASGI 서버
uvicorn[standard]>=0.24.0

# HTTP 클라이언트 (비동기)
httpx>=0.25.0

# 환경변수 관리
python-dotenv>=1.0.0

# 데이터 검증 (FastAPI에 포함되지만 명시)
pydantic>=2.0.0

# 테스트
pytest>=7.4.0
pytest-asyncio>=0.21.0
